[core]
    autocrlf = false
    eol = lf
[credential]
    helper = cache --timeout=36000
[user]
    name = Martin Mrose
[http]
    proxy = "if [[ `hostname` == W4DEUMSY9002045 ]]; then p=http://proxy.mms-dresden.de:8080; fi; $p"
[https]
    proxy = "if [[ `hostname` == W4DEUMSY9002045 ]]; then p=http://proxy.mms-dresden.de:8080; fi; $p"
